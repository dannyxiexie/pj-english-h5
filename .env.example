# Optional. If unset, lookup falls back to a fast local explanation.
LLM_API_KEY=
LLM_API_BASE=https://api.openai.com/v1
# Example: set this to the model exposed by your OpenAI-compatible provider.
LLM_MODEL=

# Used by `npm start`.
PORT=4173
